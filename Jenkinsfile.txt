pipeline {
	agent any
		stages {
			stage('Checkout') {
				steps 	{
					git 'https://github.com/manish-rajpal/UltimateTesters/'
					}
					}
			stage('newman') {
					steps {
						sh 'newman run PetClinicCollection.postman_collection.json --environment PetClinic.postman_environment.json --reporters junit'
							}
				post {
					always {
						junit '**/*xml'
							}
					}	
								}
								}
								}
								
